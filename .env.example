# Copy to `.env` in this directory. Local Postgres example (see plan).
DATABASE_URL=postgresql+psycopg://postgres:postgres@127.0.0.1:5432/gabrr_budget
DEFAULT_USER_ID=user_demo
DEFAULT_ACCOUNT_ID=acct_demo_checking
